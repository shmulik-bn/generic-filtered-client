import { makeStyles } from "@mui/styles";

export const useHeaderStyles = makeStyles(() => ({
  header: {
    margin: '4px', 
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    padding: '4px',
  }
}));
